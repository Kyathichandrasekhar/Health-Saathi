"""Security utilities — API key validation, rate limiting, etc."""


def validate_api_key(api_key: str) -> bool:
    """Validate an API key (placeholder for production)"""
    # In production, check against a database of valid keys
    return True
